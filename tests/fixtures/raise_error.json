{
  "name": "raise_error",
  "pyspark_version": "3.5.0",
  "input": {
    "schema": [
      { "name": "id", "type": "bigint" }
    ],
    "rows": [[1], [2]]
  },
  "operations": [
    { "op": "withColumn", "column": "err", "expr": "raise_error('intentional failure')" }
  ],
  "expected": {
    "schema": [],
    "rows": []
  },
  "skip": true,
  "skip_reason": "raise_error fails when evaluated; fixture exercises expression parsing only."
}
