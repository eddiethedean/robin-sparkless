{
  "name": "date_from_unix_date",
  "pyspark_version": "3.5.0",
  "input": {
    "schema": [
      { "name": "id", "type": "bigint" },
      { "name": "days", "type": "int" }
    ],
    "rows": [
      [1, 16533],
      [2, 0]
    ]
  },
  "operations": [
    { "op": "withColumn", "column": "dt", "expr": "date_from_unix_date(col('days'))" },
    { "op": "select", "columns": ["id", "days", "dt"] },
    { "op": "orderBy", "columns": ["id"], "ascending": [true] }
  ],
  "expected": {
    "schema": [
      { "name": "id", "type": "bigint" },
      { "name": "days", "type": "int" },
      { "name": "dt", "type": "date" }
    ],
    "rows": [
      [1, 16533, "2015-04-08"],
      [2, 0, "1970-01-01"]
    ]
  }
}
